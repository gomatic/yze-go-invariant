package a_test

import "testing"

// TestBlind names the unexported blind from the external test package. It could
// not write that name if it tried, so the mention is not required of it.
func TestBlind(t *testing.T) {}
